### Neden Docker Kullanıyoruz?
- **Docker bir SQL aracı veya veritabanı değildir.** Sadece uygulamaları yalıtılmış bir "kutu" (container) içinde çalıştırmaya yarayan bir araçtır.
- **Neden veritabanı için kullandık?** PostgreSQL'i bilgisayara doğrudan kurmak (ayarlar, arka plan servisleri vb.) karmaşıktır ve bilgisayarı yavaşlatabilir.
- Docker sayesinde internetten "içinde PostgreSQL hazır kurulu olan bir kutu" indirdik ve çalıştırdık.

### pgx Nedir?
- `pgx`, Go dili için geliştirilmiş modern ve yüksek performanslı bir PostgreSQL sürücüsüdür (driver).
- **Mantığı:** Bizim yazdığımız Go kodları (ve `database/sql` kütüphanesi) doğrudan veritabanıyla nasıl konuşacağını bilmez. `pgx`, araya girerek Go komutlarımızı PostgreSQL'in anlayacağı dile çeviren bir "tercüman" görevi görür.
- `_ "github.com/jackc/pgx/v5/stdlib"` şeklindeki kullanım (`_` alt çizgi ile), sürücünün sadece arka planda kendini sisteme kaydetmesini (init fonksiyonunun çalışmasını) sağlar. Bu sayede `sql.Open("pgx", dsn)` dediğimizde sistem pgx'i tanıyabilir.
go.mod projenin bağımlılıklarını tutuyor. go get komutu ile eklediğimiz paketler buraya kaydediliyor.

go.sum ise go.mod içindeki paketlerin hash kodlarını tutuyor. Bu sayede paketlerin güvenliği sağlanıyor.

### .env ve godotenv Nedir?
- **.env:** Veritabanı şifresi, port numarası, JWT gizli anahtarı gibi gizli veya ortama göre değişen ayarları kodun içine yazmak yerine sakladığımız çevre değişkenleri (environment variables) dosyasıdır.
- **Neden .gitignore'a eklenir?** Şifrelerin GitHub'a sızmasını engellemek için `.env` gizlenir. Takım arkadaşlarının hangi değişkenleri tanımlaması gerektiğini görmesi için örnek olarak `.env.example` paylaşılır.
- **godotenv:** Go'da `.env` dosyasındaki satırları okuyup işletim sisteminin `os.Getenv(...)` fonksiyonu ile erişilebilir hale getiren popüler bir kütüphanedir.
- **Frontend (Vite):** React tarafında değişkenler `VITE_` önekiyle başlar (örn. `VITE_API_URL`) ve kodda `import.meta.env.VITE_API_URL` şeklinde okunur.

### db/init.sql ile repository/db.go Farkı Nedir?
- **db/init.sql:** Veritabanının mimari planıdır (Blue-print). Docker veya PostgreSQL ilk kurulurken çalışır ve tüm tabloları (`users`, `listings`, `bids`, `messages`) sıfırdan kurar. Tablo şemalarının tek ve asıl kaynağıdır.
- **repository/db.go:** Go uygulamasının PostgreSQL sunucusuna fiziksel bağlantıyı açmasını sağlayan (`ConnectDB`) saf bağlantı sağlayıcıdır. Tablo tanımları içermez, sadece bağlantı havuzunu (`*sql.DB`) üretir.
- **repository/*_repo.go:** `db.go`'nun kurduğu o bağlantıyı kullanarak ilgili tablolara SQL sorguları atan (`UserRepository`, `ListingRepository` vb.) modüllerdir.



